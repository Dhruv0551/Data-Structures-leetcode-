class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        // bool isOdd = false;

        // for(int x: nums1)
        // {
        //     if(x % 2 != 0)
        //     {
        //         isOdd = true;
        //         break;
        //     }
        // }

        // if (!isOdd) return true;
        return true;
    }
};